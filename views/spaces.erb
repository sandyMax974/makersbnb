<header>
  <title>MakersBnB / Home</title>
  <link rel="stylesheet" href="style.css">
</header>

<body>
<div class=fixed_header>
  <div class=header-grid>
    <div></div>
    <div class=header-text><b>MakersBnB<b>
    </div>

      <div class=signin-text><b>
        <% if !User.logged? %>
        <a href='/login'>Sign In<b></a>
        <%else%>
          <a href="/session/destroy">Sign Out</a>
        <%end%>
      </div>

  </div>
</div>

<br>
<% if User.logged? %>
  <form action="/spaces/new" method="get">
    <input type="submit" value="Add listing">
  </form>
<%end%>

<div class=column>

<h1>Listings</h1>
  <a href='/' ><div class=listing>
    <b><%= listing.title %>
      <b>
        <br>
        <br>
        <%= listing.description %>
        <p class=price>£50 per night</p>
      </div></a>
</body>

<div class="">
  <% @listings.each do |listing| %>
    <%= listing.title %>
    <% if !User.logged? %>
    <form action="/confirmation/<%= listing.id %>" method="post">
      <input type="submit" value="book <%= listing.title %>">
    </form> <%end%>
    <%= listing.description %>
  <% end %>
</div>


<h1>MakersBnB<h1>

<h2>Hello <%= @name %><h2>

<%# <% if User.logged? %>
  <form action="/spaces/new" method="get">
    <input type="submit" value="Add listing">
  </form>
<%end%> %>

<%# <% if !User.logged? %>
  <form class="" action="/login" method="get">
    <input type="submit" value="Log in">
  </form>
<%end%>

<% if User.logged? %>
  <form class="" action="/session/destroy" method="post">
    <input type="submit" value="Signout">
  </form>
<%end%> %>

<p>Listings</p>

<ul>
  <% @listings.each do |listing| %>
  <li> <%= listing.title %>
    <% if !User.logged? %>
    <form action="/confirmation/<%= listing.id %>" method="post">
      <input type="submit" value="book <%= listing.title %>">
    </form> <%end%>
    <%= listing.description %>
  </li>
  <% end %>
</ul>
